/**QUESTION:How do we subscribe to an Observable in HTML template rather than using subscribe **/

import { Component } from '@angular/core';

@Component({
  selector: 'question3',
  template: ` <h3>Q3.How do we subscribe to an Observable in HTML template rather than using subscribe</h3>
  
  `,
})
export class Question3Component {}
